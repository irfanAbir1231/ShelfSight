# Trained models

Training writes `product_facings.pt` and `square_logo.pt` here. On the next API
restart ShelfSight loads them automatically. Keep weights outside source control
and preserve the matching dataset/version metadata with production releases.
